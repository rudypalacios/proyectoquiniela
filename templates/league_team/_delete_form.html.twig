<form method="post" action="{{ path('app_league_team_delete', {'id': league_team.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ league_team.id) }}">
    <button class="btn">Delete</button>
</form>
