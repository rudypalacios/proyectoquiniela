{% extends 'base.html.twig' %}

{% block title %}LeagueMember{% endblock %}

{% block body %}
    <h1>LeagueMember</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ league_member.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ league_member.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_league_member_index') }}">back to list</a>

    <a href="{{ path('app_league_member_edit', {'id': league_member.id}) }}">edit</a>

    {{ include('league_member/_delete_form.html.twig') }}
{% endblock %}
