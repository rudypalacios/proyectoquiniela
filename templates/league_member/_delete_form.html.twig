<form method="post" action="{{ path('app_league_member_delete', {'id': league_member.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ league_member.id) }}">
    <button class="btn">Delete</button>
</form>
